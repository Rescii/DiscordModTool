import { Options } from '~/types';
export declare const defaultOptions: Options;
export default function openTerminal(command: string | string[], options?: Partial<Options>): Promise<any>;
export * from '~/types';
