export interface HashMap<T = any> {
    [key: string]: T;
}
export declare type Terminal = string[];
export declare type Terminals = HashMap<Terminal[]>;
export interface Options {
    commandTemplate: string;
    cwd: string;
    terminals: Terminals;
}
